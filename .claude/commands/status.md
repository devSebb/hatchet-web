---
description: What's changed, what's published, and what teammates have been doing
---

A plain-English picture of where things stand. **No git output.**

Cover four things:

1. **Unpublished work** — files changed since the last publish, described as
   pages and content rather than paths:
   > You've changed the About page headline and two paragraphs on
   > /solutions/discovery. These aren't published yet.

2. **Whether it would pass** — quickly run the design check and type check so
   you can say whether it is ready:
   > Everything looks fine — ready to publish whenever you are.

3. **Teammates** — recent publishes to `staging` that are not theirs, by name:
   > Since your last session: Mark published new pricing copy (yesterday), and
   > Alessandra swapped two team photos (this morning).

4. **Waiting on Seb** — any open pull request from `staging` to `main`:
   > There's one batch waiting for Seb's review, sent Tuesday.

If there is nothing to report, say so in one line. Do not pad it.
