.next
node_modules
pnpm-lock.yaml

# Generated by scripts/optimize-images.mjs — rewritten on every run, so
# formatting it would break `format:check` after each regeneration.
lib/image-variants.json
public/_img

# Generated by `pnpm sync:wp` — rewritten on every content sync, so formatting
# them would break `format:check` after each run.
lib/remote-image-sizes.json
lib/content/generated
